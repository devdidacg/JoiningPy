# JoiningPy

JoiningPy is a terminal-based Python library manager that lets users install or remove multiple Python packages through a simple interactive menu without typing commands manually.

# Installation

With Git (Recomended):
```bash
git clone "https://github.com/devdidacg/JoiningPy"
```
